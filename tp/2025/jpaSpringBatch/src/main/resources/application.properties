spring.application.name=jpaSpringBatch
#spring.profiles.default=h2
spring.profiles.default=mysql
#spring.profiles.default=postgresql

spring.batch.jdbc.initialize-schema=always

logging.level.org=INFO
logging.level.tp.tpSpringBatch=DEBUG

#enable or disable auto lauching of jobs (spring-boot-starter-batch)
#spring.batch.job.enabled=true
spring.batch.job.enabled=false
#spring.batch.job.name=myHelloWorldJob
#if not .enabled=false the property spring.batch.job.name must be set in case of multiple jobs

